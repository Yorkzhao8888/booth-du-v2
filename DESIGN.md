# DESIGN.md

## 品牌与视觉方向
- 意象锚点：**紫色渐变星空下的工作台**——Booth 登录页既有品牌渐变（#667eea→#764ba2，135deg）为全系统视觉根，门户层（容器分流/帽选择）延续该渐变作为背景，内容承载于白色卡片。
- 容器徽标语言：容器用 `#xhpz`（个人版）/`#xepz`（企业版）等井号编码展示，等宽字体、小圆角深色标签，卡片左上角作为身份锚点。

## Design Tokens
### 色彩
- 品牌渐变：linear-gradient(135deg, #667eea 0%, #764ba2 100%)（登录/门户背景）
- 卡片底：#fff，圆角 12px；主操作色跟随 AntD 默认蓝
- 置灰预留卡：#fafafa 底 + #bbb 文案 + Tooltip「预留」

### 字体
- 容器编码：等宽（SFMono/Consolas/monospace）
- 正文：系统默认中文字体栈

## 布局与响应式
- 门户页（分流/帽选择）：居中列布局，maxWidth 960；卡片网格 2 列（≥768px）/1 列（375px）
- 双端工作台（#xhpz/#xepz）：375px 单列可用；工作台头部固定显示当前容器徽标+当前帽+切换角色/切换端入口

## 组件规范
- 帽卡片：AntD Card 可点选，选中显示主色边框；默认帽右上角 Tag「默认」
- 无权限友好页：居中图标+说明+返回分流页按钮，禁止白屏

## 交互与状态
- 链路：登录 → /containers 分流 → /{container}/hats 帽选择 → /{container} 工作台
- 切换角色 = 回帽选择页重进，视角状态清空重建
- 占位入口（P1 未接）：点击 message.info 提示「P1 开放」，不空跳
